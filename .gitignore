node_modules/
dist/
.env.local
*.local
.DS_Store

# Claude Code : réglages locaux/perso (garder launch.json partagé)
.claude/settings.local.json

# Artefacts de test générés
scripts/out/

# Hash du mot de passe Basic Auth (ne pas versionner ; reste copié dans dist/ au build)
public/.htpasswd
